# Commands

- No application commands are verified yet. After Lovable scaffolds the frontend, use only scripts defined in `package.json` and update this section.

# Rules

- Treat `_docs/plan.md` as the source of truth for version 1.
- Keep version 1 client-side: React, TypeScript, and browser `localStorage`; no backend.
- Ask before adding dependencies.
- Never commit credentials or expose them to frontend code.
